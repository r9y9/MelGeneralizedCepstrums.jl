language: julia
os:
  - linux
  - osx
julia:
#  - release
  - nightly
notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia --check-bounds=yes -e 'Pkg.init(); Pkg.clone(pwd()); Pkg.clone("https://github.com/r9y9/SPTK.jl"); Pkg.build("SPTK"); Pkg.test("MelGeneralizedCepstrums", coverage=true)'
after_success:
- julia -e 'cd(Pkg.dir("MelGeneralizedCepstrums")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
